{
  "name": "platform-public-site",
  "version": "1.0.0",
  "private": true,
  "description": "Public static 3D/AR product showcase site (RTL/Persian) — built with Vite + React + TypeScript, deployed to GitHub Pages. Reads /public-data/*.json generated by the backend publish pipeline; never calls the private backend API.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/validate-public-data.mjs && vite build && node scripts/postbuild.mjs",
    "preview": "vite preview",
    "validate": "node scripts/validate-public-data.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.6"
  }
}
