import React from "react";
import { useData } from "../lib/data";
import { useSeo } from "../lib/seo";

export default function About() {
  const { settings } = useData();
  const platformName = settings?.platformName ?? "پلتفرم نمایشگاه محصول";
  useSeo({ title: "درباره ما", description: `درباره ${platformName}` });

  return (
    <div className="container section simple-page">
      <h1>درباره {platformName}</h1>
      <p>
        {platformName} یک ویترین چندفروشنده برای نمایش محصولات به‌صورت سه‌بعدی و واقعیت افزوده (AR) است.
        هدف ما این است که پیش از هر تصمیم خرید، مشتریان بتوانند محصول را از هر زاویه ببینند و با اندازه واقعی
        در فضای خودشان تجسم کنند.
      </p>
      <p>
        این پلتفرم صرفاً محل معرفی و نمایش محصولات فروشندگان مختلف است و امکان ثبت سفارش، پرداخت آنلاین یا
        تسویه‌حساب از طریق آن وجود ندارد. قیمت‌گذاری، کیفیت، زمان تحویل، گارانتی، مرجوعی و خدمات پس از فروش
        کاملاً بر عهده هر فروشنده است و پلتفرم در این موارد مسئولیتی نمی‌پذیرد.
      </p>
      <p>
        برای خرید یا دریافت اطلاعات بیشتر درباره هر محصول، لطفاً مستقیماً از طریق اطلاعات تماس موجود در صفحه
        هر فروشنده با او در ارتباط باشید.
      </p>
    </div>
  );
}
