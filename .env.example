# Optional — leave unset for a fully static site with no backend dependency.
# Only needed if you want anonymous product/AR analytics events to be
# beaconed to the backend's POST /api/analytics/track endpoint.
VITE_API_URL=
